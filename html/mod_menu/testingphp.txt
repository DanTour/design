<li class="<?php echo $class ?>"
<?php 
	if ($item->parent) {
		 echo " dropdown";
	}
	
 ?>">
	<a tableindex="0" data-toogle="dropdown" data-submenu aria-expanded="true" ><?php echo $item->title; ?>
	<?php if ($item->parent && $item->level < 2)  { ?>
		<span class="caret"></span>
	<?php } ?>
	</a>
	
	<ul class="dropdown-menu">
		<li class="dropdown-submenu">
			<a tabindex="0"><?php $item->title; ?></a>
		</li>
	</ul>	
	<?php
		if ($item->level > 1) { ?>
						
			
			
		<?php }
	?>
</li>
	
	
<?php
	}
?>


$class = 'item-'.$item->id;
		var_dump($item->level);
		//echo '<br>';
		$class = 'item-'.$item->id;
		if ($item->id == $active_id)
		{
			$class .= ' current';
		}

		if (in_array($item->id, $path))
		{
			$class .= ' active';
		}
		?> 
		<li class="<?php echo $class ?>" 
		
		<?php if ($item->level < 2 ) {
				$class .= ' dropdown';
		
		}
		else {
			$class = ' dropdown submenu';
			
		}
		
		?>
	
	<?php 
	
	
	
	
	<?php
	defined('_JEXEC') or die;
?>
<ul class="nav navbar-nav" >
<?php
	
	//var_dump($list[4]);
	
	foreach ($list as $i => &$item) { 
	//var_dump($item->id."   item deeper =>".$item->deeper.'  item level => '.$item->level);
	//echo '<br>';
		
	$class = 'item-'.$item->id;

	if ( $item->deeper ) {
		if ($item->level > 1){
				$class .= ' dropdown-submenu';
		}
		else {
			$class .=' dropdown';
		}
		
	}
	if (!empty($class)) {
		$class = ' class="'.trim($class) .'"';
	}
	?><li <?php $class ?>>
	<?php 
		if ($item->deeper) {
			
		}
	?>
	<?php }?>
</ul>
